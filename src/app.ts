// Centralna konfiguracja aplikacji. Zastępuje .env / import.meta.env —
// URL backendu jest wpisany na stałe, bez dotenv i bez zmiennych VITE_*.
export const url_backend: string = "http://127.0.0.1:8000";
